<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="goal.mapper.GroupSMapper">
	<insert id="insertGroupUser" parameterType="goal.vo.GroupSVO">
		<selectKey keyProperty="sno" resultType="int" order="BEFORE">
			select GK_SGROUP_SEQ.nextval from dual
		</selectKey>
		insert into
		GK_SGROUP(sno, uno, gno, g_role) values
		(GK_SGROUP_SEQ.currval, #{uno}, #{gno},
		#{g_role})
	</insert>
	<select id="fineUserbyId" parameterType="int" resultType="int">
		select * from GK_SGROUP where gno=#{gno}
	</select>
	
</mapper>