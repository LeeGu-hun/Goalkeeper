<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goal.mapper.UserMapper">
	<select id="read" parameterType="String" resultType="goal.vo.UserVO">
		select u_id, u_name, u_password, u_profile, u_mail, u_number, u_gender, u_birthdate from GK_USER
		where u_id = #{u_id}
	</select>
	
	<insert id="regeister" parameterType="goal.vo.UserVO">
		insert into GK_USER(uno, u_id, u_name, u_password, u_mail, u_number, u_birthdate) values
		(GK_USER_SEQ, #{u_id}, #{u_name}, #{u_password}, #{u_mail}, #{u_number}, #{u_birthdate})
	</insert>
</mapper>