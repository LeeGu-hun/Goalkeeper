<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goal.mapper.GroupDataMapper">
	<insert id="insertData" parameterType="goal.vo.GroupDataVO">
		insert into
		GK_ALLGROUP(gno, uno, hor_data, ver_data) values
		(GK_ALLGROUP_SEQ.nextval, #{uno}, #{hor_data}, #{ver_data})
	</insert>
</mapper>